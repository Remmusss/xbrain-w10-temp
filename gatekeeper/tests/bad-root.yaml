apiVersion: v1
kind: Pod
metadata:
  name: bad-root
  namespace: demo
spec:
  containers:
    - name: app
      image: nginx:1.27.0
      securityContext:
        runAsUser: 0
      resources:
        limits:
          cpu: 100m
          memory: 64Mi
